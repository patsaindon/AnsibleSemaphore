---

- name: Test task
  gather_facts: false
  hosts: all
  vars:
    container_name: "radarr"

  tasks:
    # FACTS ##################################################################                                                                                                             #####
    - name: Update Extended Scripts
      shell: |
        wget 'https://raw.githubusercontent.com/RandomNinjaAtk/arr-scripts/main/{{ container_name }}/extended.conf' >> '/mnt/user/appdata/extended.conf'
    
